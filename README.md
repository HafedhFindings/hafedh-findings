# hafedh-findings

Public catalogs on the Hostinger VPS (`193.203.169.176`, `srv1915148.hstgr.cloud`).

The home page lists Media and Products. `/media/` and `/products/` are the full pages. Each item shows the original Facebook link.

Docker: `docker compose up -d --build`
